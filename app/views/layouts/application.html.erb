<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Biblioteca Municipal" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Biblioteca Municipal">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "formulario", "global", "header", "login", "tabelas", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if current_bibliotecario && !current_bibliotecario.senha_provisoria %>
      <header class="header">
        <div class="logo">
          <%= image_tag "logo.png", width: 180, alt: "Logo" %>
        </div>
        <nav class="menu-area">
          <%= link_to "Empréstimos", emprestimos_path, class: "btn-menu" %>
          <%= link_to "Livros", livros_path, class: "btn-menu" %>
          <%= link_to "Categorias", categorias_path, class: "btn-menu" %>
          <%= link_to "Usuários", usuarios_path, class: "btn-menu" %>
          
          <% if current_bibliotecario.is_admin %>
            <%= link_to "Bibliotecários", bibliotecarios_path, class: "btn-menu" %>
          <% end %>

          <%= button_to "SAIR", destroy_bibliotecario_session_path, method: :delete, class: "btn-menu", data: { turbo: false } %>
        </nav>
      </header>
    <% end %>

    <main class="<%= devise_controller? ? '' : 'content' %>">
      <%= yield %>
    </main>
  </body>
</html>